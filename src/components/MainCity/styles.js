import styled from "styled-components";
import { Card } from "antd";

const MainCityCard = styled(Card)`
  &&&&& {
    background-color: rgba(1, 50, 67, 1);
    color: #fff;
  }
`;

export { MainCityCard };
